{
  "name": "node-ipc-poc",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "node-ipc": "^9.1.1"
  },
  "dependencies": {
    "colors": "^1.3.2",
    "debug": "^4.1.0"
  },
  "scripts": {
    "server": "pm2 start ./src/server.js -i 2 && pm2 logs server",
    "workers": "pm2 start ./src/worker.js -i 4 && pm2 logs worker"
  }
}
